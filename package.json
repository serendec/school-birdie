{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "concurrently \"php artisan serve --host=0.0.0.0 --port=8000\" \"mailpit --smtp 127.0.0.1:1025 --listen 127.0.0.1:8025\"",
        "start": "npm run serve"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "concurrently": "^8.2.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
